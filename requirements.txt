# 1:100万基础地理信息PostGIS MCP服务依赖
# China 1M GeoData PostGIS MCP Service Dependencies
# 
# 安装方法:
# pip install -r requirements.txt
#
# 注意: 在Windows上，fiona和GDAL可能需要额外步骤:
# 1. 使用conda: conda install -c conda-forge gdal fiona shapely psycopg2
# 2. 或下载OSGeo4W: https://trac.osgeo.org/osgeo4w/

# MCP核心库
mcp>=0.9.0

# 地理数据处理
fiona>=1.9.0
psycopg2-binary>=2.9.0
shapely>=2.0.0

# 配置和工具
configparser>=5.0.0
pyyaml>=6.0.0

# 缓存支持（可选）
#redis>=4.0.0  # 如需使用Redis缓存，取消注释

# 测试依赖（开发环境）
 #pytest>=7.0.0
 #pytest-asyncio>=0.21.0
 #pytest-cov>=4.0.0  # 代码覆盖率

